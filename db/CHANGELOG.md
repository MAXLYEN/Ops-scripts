# 版本迭代记录

各文件独立编号。本次按现有头注释建立目录记录；旧注释未标日期的版本保持日期未记载，不补造历史。

## 2026-09-25：低风险问题清理

| 文件 | 原版本 → 当前版本 | 变更 |
| --- | --- | --- |
| `rotate-db-pass.sh` | 2.0.3 → 2.0.4 | 校验经 `NEWPASS` 传入的密码，含 `' \ @ : /` 时拒绝（会破坏 SQL 语句或连接串） |

## 2026-09-24：中风险问题修复

| 文件 | 原版本 → 当前版本 | 变更 |
| --- | --- | --- |
| `rotate-db-pass.sh` | 2.0.2 → 2.0.3 | 任一 host 的 `ALTER USER` 失败即中止：不改下游连接串、重启已停的容器，并提示用 `NEWPASS=` 以同一密码重跑。原先照样把新密码写进下游，服务会连不上库 |

## 2026-09-24：凭据移出进程命令行

同机任何用户都能读 `/proc/*/cmdline`；面板上以 www 运行的站点被攻破后，可在任务运行窗口拿到凭据。

| 文件 | 原版本 → 当前版本 | 变更 |
| --- | --- | --- |
| `rotate-db-pass.sh` | 2.0.1 → 2.0.2 | `ALTER USER` 语句改经 stdin 交给 mysql，新密码不再出现在 `mysql -e` 的参数里 |

## 2026-09-24：注释与目录文档整理

| 文件 | 原版本 → 当前版本 | 变更 |
| --- | --- | --- |
| `rotate-db-pass.sh` | 2.0.0 → 2.0.1 | 精简注释并修正帮助输出 |
| `sqlite-dsn.sh` | 2.0.0 → 2.0.1 | 精简注释并修正帮助输出 |

历史条目仅迁移原脚本头部已有的版本说明。保留在正文中的注释用于解释关键判断或写入配置文件。

后续更新按文件记录版本、日期、改动原因和行为变化。
