# 版本迭代记录

本目录各脚本独立编号。下列历史版本来自原脚本头注释；原注释未记录日期，因此不补造日期。

## 2026-09-24：注释与文档整理

| 脚本 | 原版本 → 当前版本 | 本次变更 |
| --- | --- | --- |
| `run.sh` | 2.0.0 → 2.0.1 | 精简头注释，保留调度用法和阶段 03 安全提示 |
| `00-precheck.sh` | 1.0.1 → 1.0.2 | 精简双栈/NAT 说明 |
| `01-swap-memory.sh` | 1.1.0 → 1.1.1 | 收敛 fstab 校验说明 |
| `02-system-network.sh` | 1.1.0 → 1.1.1 | 收敛端口来源及 fstab 校验说明 |
| `03-ssh-firewall.sh` | 1.3.0 → 1.3.1 | 保留 SSH 安全提示，精简正文注释 |
| `04-verify.sh` | 1.1.0 → 1.1.1 | 收敛只读验收及 fstab 校验说明 |

本次只调整注释、版本号和目录文档，未修改执行逻辑。写入系统配置文件的注释保持原样。

## 既有版本（日期未记录）

- `run.sh` 2.0.0：改用 `opsget` 拉取阶段脚本，不再依赖 `/root/s0*.sh`。
- `00-precheck.sh` 1.0.1：IPv4 与 IPv6 分别探测并与同协议族网卡地址比较，避免双栈机器误报 NAT。
- `01-swap-memory.sh` 1.1.0：按真实挂载错误的增量判断是否回滚，避免 Debian 模板条目使回滚判断失效。
- `02-system-network.sh` 1.1.0：fstab 校验区分真实问题与已知无害项。
- `03-ssh-firewall.sh` 1.3.0：增加交互式 SSH 会话的 `TMOUT` 空闲超时，可通过 `SSH_IDLE_TIMEOUT` 覆盖或设为 0 关闭；长任务不受影响。
- `03-ssh-firewall.sh` 1.2.0：端口段支持 `env.conf` 覆盖，按配置生成容器数据库放行规则。
- `03-ssh-firewall.sh` 1.1.0：移除 `61000:62000` 端口段，启用 ufw 后检查 Docker iptables 链。
- `04-verify.sh` 1.1.0：fstab 校验区分真实挂载错误与 Debian 光驱模板、swapfile 语义误报。

后续按脚本记录版本、日期、改动原因和行为变化。
